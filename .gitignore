.claude
.cursor
.DS_Store
.idea
.npmrc
.vscode
dist
node_modules

# Local reference material, not part of this project:
#   VolumetricVideo   - a separate repository with its own history
#   supersplat-main   - pristine upstream copy, kept for diffing
Repos

# the bridge's self-provisioned COLMAP
bridge/.colmap/
